<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hitman.sang">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".liste_donneur" />
        <activity android:name=".info_donneur" />
        <activity android:name=".connection" />
        <activity android:name=".inscription" />
        <activity android:name=".utilisateur" />
        <activity android:name=".admin" />
        <activity android:name=".message" />
        <activity android:name=".liste_donneurs" />
        <activity android:name=".liste_donnations" />
        <activity android:name=".etat" />
        <activity android:name=".message1" />
        <activity android:name=".etat1"></activity>
    </application>

</manifest>